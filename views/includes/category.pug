div.list-group
    if categories.length > 0
        each category in categories
            a(href=`/categories/${category.name}` class=path===`/categories/${category.id}`?"list-group-item list-group-item-action active":"list-group-item list-group-item-action") #{category.name}
